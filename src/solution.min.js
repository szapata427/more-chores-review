document.addEventListener('DOMContentLoaded',()=>{function b(f){console.log(f.target.value)}const d=document.getElementById('chore-list'),e=document.getElementById('new-chore-form');e.addEventListener('submit',function(f){f.preventDefault();const g=document.getElementById('title').value,h=document.getElementById('priority').value,i=document.getElementById('duration').value;fetch('http://localhost:3000/chores',{method:'POST',headers:{Accept:'applicaton/json','Content-Type':'application/json'},body:JSON.stringify({title:g,priority:h,duration:i})}).then(j=>j.json()).then(j=>{let k=document.createElement('div');k.innerHTML=`
        <button class='delete-button' data-id=${j.id}>x</button>
        <h3> ${j.title} </h3>
        <p> ${j.duration} </p>
      `;let l=document.createElement('input');l.value=j.priority,l.addEventListener('blur',b),k.append(l),k.className='chore-card',d.append(k)})}),d.addEventListener('click',function(f){if('delete-button'==f.target.className){f.target.parentElement.remove();let g=f.target.getAttribute('data-id');fetch(`http://localhost:3000/chores/${g}`,{method:'DELETE'})}}),fetch('http://localhost:3000/chores').then(f=>f.json()).then(f=>{f.forEach(g=>{let h=document.createElement('div');h.innerHTML=`
          <button class='delete-button' data-id=${g.id}>x</button>
          <h3> ${g.title} </h3>
          <p> Duration: ${g.duration} </p>
        `;let i=document.createElement('input');i.value=g.priority,i.addEventListener('blur',b),h.append(i),h.className='chore-card',d.appendChild(h)})})});
